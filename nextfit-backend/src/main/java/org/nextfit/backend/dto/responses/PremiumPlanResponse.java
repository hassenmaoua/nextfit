package org.nextfit.backend.dto.responses;

import org.nextfit.backend.model.PremiumPlan;
import org.nextfit.backend.model.Plan;

public class PremiumPlanResponse extends Plan<PremiumPlan> {
    // Can add custom logic if needed
}